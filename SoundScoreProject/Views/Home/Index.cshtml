<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SoundScore - Your Music Discovery Platform</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif; /* Modern, readable font */
            background-color: #f8f9fa; /* Light gray background */
            color: #333; /* Dark gray text for better readability */
        }

        .navbar {
            background-color: #343a40; /* Dark navbar */
        }

        .navbar-brand, .navbar-nav .nav-link {
            color: white;
        }

            .navbar-brand:hover, .navbar-nav .nav-link:hover {
                color: #17a2b8;
            }

        .hero {
            background: linear-gradient(135deg, #667eea, #764ba2); /* Gradient background */
            color: white;
            padding: 120px 0; /* More padding */
            text-align: center;
            position: relative;
            overflow: hidden; /* Hide overflowing background elements */
        }

            .hero h1 {
                font-size: 3.5rem; /* Larger heading */
                font-weight: 700; /* Bolder heading */
                margin-bottom: 20px;
                position: relative;
                z-index: 2; /* Ensure text is above pseudo-elements */
            }

            .hero p {
                font-size: 1.3rem;
                margin-bottom: 40px;
                position: relative;
                z-index: 2;
            }

        .btn-primary {
            background-color: #fff; /* White button */
            color: #764ba2; /* Text color matches gradient */
            border: 2px solid #fff; /* White border */
            padding: 12px 30px; /* Larger button */
            font-weight: 600;
            transition: all 0.3s ease; /* Smooth transition */
            border-radius: 50px;
            /
        }

            .btn-primary:hover {
                background-color: #764ba2; /* Inverted colors on hover */
                color: #fff;
                border-color: #764ba2;
            }

        /* Abstract background shapes (optional, but add visual interest) */
        .hero::before, .hero::after {
            content: '';
            position: absolute;
            width: 200px;
            height: 200px;
            border-radius: 50%;
            z-index: 1; /* Behind text, but above gradient */
        }

        .hero::before {
            background-color: rgba(255, 255, 255, 0.2); /* Semi-transparent white */
            top: -50px;
            left: -50px;
        }

        .hero::after {
            background-color: rgba(255, 255, 255, 0.1);
            bottom: -50px;
            right: -50px;
        }

        /* Features Section (NEW) */
        .features {
            padding: 80px 0;
            text-align: center;
        }

        .feature-icon {
            font-size: 3rem;
            color: #764ba2; /* Match brand colors */
            margin-bottom: 20px;
        }

        .feature-title {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 10px;
        }

        footer {
            background-color: #343a40;
            color: #fff;
            padding: 20px 0;
            text-align: center;
            margin-top: 40px; /* Space from content above */
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand" href="#">SoundScore</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/Albums">Browse Albums</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">About</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Contact</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="hero">
        <div class="container">
            <h1>Welcome to SoundScore</h1>
            <p>Explore a world of music and discover your next favorite album.</p>
            <a href="/Albums" class="btn btn-primary btn-lg">Browse Albums</a>
        </div>
    </div>

    <section class="features">
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    <div class="feature-icon">🎧</div>
                    <h3 class="feature-title">Curated Collections</h3>
                    <p>Discover hand-picked albums and playlists to suit your mood.</p>
                </div>
                <div class="col-md-4">
                    <div class="feature-icon">🔍</div>
                    <h3 class="feature-title">Powerful Search</h3>
                    <p>Find exactly what you're looking for with our advanced search tools.</p>
                </div>
                <div class="col-md-4">
                    <div class="feature-icon">🌟</div>
                    <h3 class="feature-title">Personalized Recommendations</h3>
                    <p>Get tailored suggestions based on your listening history.</p>
                </div>
            </div>
        </div>
    </section>
    <footer>
        <div class="container">
            <p>&copy; 2023 SoundScore. All rights reserved.</p>
        </div>
    </footer>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>